import React, {Fragment} from 'react';
import { Search, Grid } from 'semantic-ui-react';

// Hard code for now


const SearchBar = () => {
    return (
        <Grid.Row centered>
           <Search />   

        </Grid.Row>
    );  
}

export default SearchBar;